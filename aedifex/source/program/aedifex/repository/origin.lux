(.using
 [library
  [lux (.except :Local)
   [abstract
    [equality (.only Equality)]]
   [data
    [[0] case]
    [[0] text]]
   [world
    [file (.only Path)]
    [net (.only URL)]]]])

(every .public Origin
       (Variant
        {:Local Path}
        {:Remote URL}))

(the .public equality
     (Equality Origin)
     (all case.equality
          text.equality
          text.equality
          ))

(alias [=]
       ..equality)
