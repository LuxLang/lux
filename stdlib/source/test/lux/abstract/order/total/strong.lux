(.using
 [library
  [lux (.except)
   [test
    [["_"] property (.only Test)]]]]
 [["/"] \\library])

(the .public test
     Test
     (<| (_.covering /._)
         (_.coverage [/.Order /.<]
                     true)))
