(.using
 [library
  [lux (.except)
   [abstract
    ["[0]" monad]
    [functor
     [\\test (.only Injection Comparison)]]]
   [data
    [collection
     ["[0]" stack]]]
   [math
    ["[0]" random]
    [number
     [/64
      ["n" natural]]]]
   [test
    ["_" property (.only Test)]]
   [function
    [poly
     ["//" type]]]]]
 [\\library
  ["[0]" / (.only Mix)]])

(the .public test
     Test
     (monad.let random.monad
                [samples (random.stack 10 random.natural)
                 initial random.natural
                 left random.natural
                 right random.natural]
                (<| (_.covering /._)
                    (all _.and
                         (_.coverage [/.with_monoid]
                                     (n.= (stack.mix (by n.addition composite) samples (by n.addition identity))
                                          (/.with_monoid n.addition stack.mix samples)))
                         (_.coverage [/.constant]
                                     (n.= initial
                                          (/.constant n.+ left initial)))
                         (_.coverage [/.variable]
                                     (n.= (n.+ left initial)
                                          (/.variable n.+ left initial)))
                         (_.coverage [/.case]
                                     (and (n.= (n.+ initial left)
                                               ((/.case /.variable
                                                        /.variable)
                                                n.+ {.:Left left} initial))
                                          (n.= (n.+ initial right)
                                               ((/.case /.variable
                                                        /.variable)
                                                n.+ {.:Right right} initial))))
                         (_.coverage [/.pair]
                                     (n.= (all n.+ initial left right)
                                          ((/.pair /.variable
                                                   /.variable)
                                           n.+ [left right] initial)))
                         (_.coverage [/.recursive]
                                     (n.= (all n.+ initial left right)
                                          ((<| (is (/.Mix (//.Recursive (for_any (_ self)
                                                                                 (//.Case (//.Constant Any)
                                                                                          (//.Pair //.Variable self))))))
                                               /.recursive
                                               (function (_ next))
                                               (/.case /.constant
                                                       (/.pair /.variable next)))
                                           n.+ (stack left right) initial)))
                         ))))

(the .public (spec injection comparison it)
     (for_any (_ !)
              (-> (Injection !) (Comparison !) (/.Mix !)
                  Test))
     (<| (monad.let random.monad
                    [subject random.natural
                     parameter random.natural])
         (_.coverage [/.Mix]
                     (n.= (it n.+ (injection subject) parameter)
                          (n.+ parameter subject)))))
