(.using
 [library
  [lux (.except)
   [test
    [["_"] property]]]]
 [["/"] \\library])

(the .public test
     _.Test
     (<| (_.covering /._)
         (all _.and
              (_.coverage [/.let]
                          false)
              )))

