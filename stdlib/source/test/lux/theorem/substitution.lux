(.using
 [library
  [lux (.except)
   [test
    [["_"] property]]]]
 [\\library
  [[0] /]])

(the .public test
     _.Test
     (<| (_.covering /._)
         (all _.and
              (_.coverage [/.any]
                          false)
              )))
