(.using
 [library
  [lux (.except)
   [test
    [["_"] property (.only Test)]]]]
 [[0] /
  [[1 0] bit]
  [[1 0] continuous]
  [[1 0] fuzzy]])

(the .public test
     Test
     (all _.and
          /bit.test
          /continuous.test
          /fuzzy.test
          ))
