(.module:
  [lux #*
   [host
    ["_" ruby (#+ Expression)]]]
  [//
   [//
    ["." reference]]])

(def: #export system
  (reference.system (: (-> Text (Expression Any)) _.local)
                    (: (-> Text (Expression Any)) _.local)))
