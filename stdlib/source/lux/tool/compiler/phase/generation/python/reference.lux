(.module:
  [lux #*
   [host
    ["_" python (#+ Expression)]]]
  [//
   [//
    ["." reference]]])

(def: #export system
  (reference.system (: (-> Text (Expression Any)) _.var)
                    (: (-> Text (Expression Any)) _.var)))
