(;module: lux)

## Signatures
(sig: #export (Monoid a)
  {#;doc "A way to combine (append) values.

          Includes an identity (unit) value which does not alter any other value when combined with."}
  (: a
     unit)
  (: (-> a a a)
     append))
