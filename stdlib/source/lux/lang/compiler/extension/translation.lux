(.module:
  lux
  (lux (data [text]
             (coll (dictionary ["dict" unordered #+ Dictionary]))))
  [//])

(def: #export defaults
  (Dictionary Text //.Translation)
  (dict.new text.Hash<Text>))
