(.using
  [library
   [lux "*"
    [abstract
     [monad {"+" Monad}]]
    [control
     ["[0]" try {"+" Try}]
     [concurrency
      ["[0]" async {"+" Async}]]]]])

(type: .public (Action a)
  (Async (Try a)))

(def: .public monad
  (Monad Action)
  (:expected (try.with async.monad)))
