(.module:
  [library
   [lux #*
    [abstract
     [monad (#+ Monad)]]
    [control
     ["." try (#+ Try)]
     [concurrency
      ["." async (#+ Async)]]]]])

(type: .public (Action a)
  (Async (Try a)))

(def: .public monad
  (Monad Action)
  (:assume (try.with async.monad)))
