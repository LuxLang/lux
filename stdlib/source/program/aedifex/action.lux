(.module:
  [library
   [lux #*
    [abstract
     [monad (#+ Monad)]]
    [control
     ["." try (#+ Try)]
     [concurrency
      ["." async (#+ Async)]]]]])

(type: #export (Action a)
  (Async (Try a)))

(def: #export monad
  (Monad Action)
  (:assume (try.with async.monad)))
