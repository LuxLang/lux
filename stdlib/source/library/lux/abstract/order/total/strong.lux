(.using
 [library
  [lux (.except)
   [function
    [predicate (.only Predicate)]]]])

(comment "https://en.wikipedia.org/wiki/Total_order"
         (every .public (Order it)
                (Interface
                 (is (-> it
                         (Predicate it))
                     <))))
