(.using
 [library
  [lux (.except)]])

(every .public (Functor context)
       (Interface
        (is (for_any (_ before after)
                     (-> (<- before
                             after)
                         (-> (context before)
                             (context after))))
            each)))
