(.using
 [library
  [lux (.except)]]
 [///
  [[0] index (.only Index)]])

(every .public Constant
       Index)

(the .public equality
     index.equality)

(alias [=]
       ..equality)

(the .public length
     index.length)

(the .public as_binary
     index.as_binary)
