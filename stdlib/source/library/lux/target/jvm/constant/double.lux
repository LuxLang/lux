(.using
 [library
  [lux (.except Double)
   [abstract
    [equality (.only Equality)]]
   [data
    [[0] binary
     [[1] \\injection (.only Injection)]]]
   [math
    [[0] random (.only Random)]
    [number
     [/64
      [[0] real]]]]]])

(every .public Double
       Real)

(the .public equality
     (Equality Double)
     real.equality)

(alias [=]
       ..equality)

(the .public as_binary
     (Injection Double)
     (|>> real.bits
          binary.bits_64))

(the .public positive_zero
     Double
     +0.0)

(the .public negative_zero
     Double
     -0.0)

(the .public random
     (Random Double)
     (|> random.real
         (random.only real.number?)))
