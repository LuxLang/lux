(.using
 [library
  [lux (.except)
   ["[0]" ffi]
   [abstract
    [equality (.only Equality)]]
   [data
    ["[0]" binary (.only)
     ["[1]I" \\injection (.only Injection)]]]
   [math
    ["[0]" random (.only Random) (.open functor)]
    [number
     [/64
      ["[0]" real]]]]]])

(ffi.the java/lang/Float
         ("static" floatToRawIntBits "manual" [float] int))

(every .public Float
       java/lang/Float)

(the .public as_binary
     (Injection Float)
     (|>> [] java/lang/Float:floatToRawIntBits
          ffi.of_int
          binaryI.bits_32))

(the .public equality
     (Equality Float)
     (implementation
      (the (= reference it)
           (binary.= (binaryI.value ..as_binary reference)
                     (binaryI.value ..as_binary it)))))

(alias [=]
       ..equality)

(the .public positive_zero
     Float
     (ffi.as_float +0.0))

(the .public negative_zero
     Float
     (ffi.as_float -0.0))

(the .public not_a_number
     Float
     (ffi.as_float real.not_a_number))

(the .public random
     (Random Float)
     (random:each (|>> ffi.as_float) random.real))
