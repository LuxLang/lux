(.using
 [library
  [lux (.except abstraction argument)
   [compiler
    [type (.only Register)]]]])

(the .public abstraction
     Register
     0)

(the .public argument
     Register
     (++ ..abstraction))
