(.using
 [library
  [lux (.except i64 char)
   [target
    [["_"] c++ (.only)
     [[0] type]]]]])

(the .public bit
     _.Type
     type.bool)

(the .public i64
     _.Type
     type.int_64)

(the .public f64
     _.Type
     type.double)

(the .public char
     _.Type
     type.char_32)

(the .public lefts
     _.Type
     type.char)

(the .public right?
     _.Type
     ..bit)

(the .public arity
     _.Type
     ..lefts)
