(.using
 [library
  [lux (.except)
   [target
    ["_" js (.only Expression)]]]]
 [///
  [reference (.only System)]])

(the .public system
     (System Expression)
     (implementation
      (the constant' _.var)
      (the variable' _.var)))
