(.using
 [library
  [lux (.except)
   [target
    [["_"] php (.only Expression)]]]]
 [///
  [reference (.only System)]])

(the .public system
     (System Expression)
     (implementation
      (the constant _.global)
      (the variable _.var)))
