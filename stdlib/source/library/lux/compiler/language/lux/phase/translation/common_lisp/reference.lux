(.using
 [library
  [lux (.except)
   [target
    [["_"] common_lisp (.only Expression)]]]]
 [///
  [reference (.only System)]])

(the .public system
     (System (Expression Any))
     (implementation
      (the constant _.var)
      (the variable _.var)))
