(.using
 [library
  [lux (.except Module)]]
 [/
  [descriptor (.only Descriptor)]
  [document (.only Document)]])

(every .public ID
       Natural)

(the .public runtime
     ID
     0)

(every .public (Module a)
       (Record
        [:id ID
         :descriptor Descriptor
         :document (Document a)]))
