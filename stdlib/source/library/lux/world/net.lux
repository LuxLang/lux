(.using
 [library
  [lux (.except)]]
 [/
  [uri
   [port (.only Port)]]])

(every .public Host
       Text)

(every .public URL
       Text)

(every .public Address
       (Record
        [:host Host
         :port Port]))
