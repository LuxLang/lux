(.require
 [library
  [lux (.except #host)]]
 [/
  [uri
   [port (.only Port)]]])

(type .public Host
  Text)

(type .public URL
  Text)

(type .public Address
  (Record
   [#host Host
    #port Port]))
