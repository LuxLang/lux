(.module:
  [library
   [lux (#- Location)]])

(type: .public Address
  Text)

(type: .public Port
  Nat)

(type: .public URL
  Text)

(type: .public Location
  (Record
   {#address Address
    #port Port}))
