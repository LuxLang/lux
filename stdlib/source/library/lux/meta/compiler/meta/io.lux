(.require
 [library
  [lux (.except Code)
   [data
    ["[0]" text]]
   [world
    ["[0]" file]]]]
 [//
  [archive
   [module
    ["[0]" descriptor]]]])

(type .public Context
  file.Path)

(type .public Code
  Text)

(def .public (path fs)
  (All (_ !)
    (-> (file.System !) descriptor.Module
        file.Path))
  (text.replaced .module_separator
                 (of fs separator)))
