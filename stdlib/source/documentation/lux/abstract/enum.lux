(.require
 [library
  [lux (.except)
   ["$" documentation]]]
 [\\library
  ["[0]" /]])

(def .public documentation
  (List $.Documentation)
  (list ($.module /._
                  "")

        ($.definition (/.Enum it)
          "Enumerable types, with a notion of moving forward and backwards through a type's instances.")

        ($.definition /.range
          "An inclusive [from, to] range of values."
          ($.example (range enum from to)))))
