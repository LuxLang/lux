(;module:
  lux
  (lux (control monad)
       (concurrency ["P" promise "P/" Monad<Promise>])
       (data text/format
             ["E" error]))
  (luxc ["&" base]))

(def: #export (compile-runtime _)
  (-> Top (P;Promise (E;Error Unit)))
  (P/wrap (#E;Success [])))
