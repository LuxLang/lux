(;module:
  lux
  (lux (control monad)
       [io]
       (concurrency [promise])
       [cli #+ program:]
       [test])
  (test (luxc ["_;" parser]
              (analyser ["_;" lux]))))

## [Program]
(program: args
  (test;run))
