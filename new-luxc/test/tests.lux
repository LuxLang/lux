(;module:
  lux
  (lux (control monad)
       [io]
       (concurrency [promise])
       [cli #+ program:]
       [test])
  (test (luxc ["_;" parser]
              (analyser ["_;" primitive]
                        ["_;" struct]
                        ["_;" reference]))))

## [Program]
(program: args
  (test;run))
